<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/base_colorPrimary</item>
        <item name="colorPrimaryDark">@color/base_colorPrimaryDark</item>
        <item name="colorAccent">@color/base_colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/base_text_size_dialog</item>
    </style>
    <!-- 等待圈 -->
    <style name="loadMoreViewStyle">
        <item name="android:indeterminateDrawable">@drawable/base_progress_large</item>
    </style>

    <style name="optionStyle">
        <item name="android:layout_centerVertical">true</item>
        <item name="android:background">@mipmap/icon_option_bg</item>
        <item name="android:textColor">@color/base_white</item>
        <item name="android:textSize">@dimen/base_text_size_max</item>
    </style>

    <style name="titleStyle">
        <item name="android:layout_centerVertical">true</item>
        <item name="android:textColor">@color/base_white</item>
        <item name="android:textSize">@dimen/base_text_size_dialog</item>
    </style>
</resources>
